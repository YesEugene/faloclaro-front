# Файлы, которые можно изменять при разработке CRM

## ✅ МОЖНО ТРОГАТЬ (Курс /pt)

### Страницы курса
- `/app/pt/page.tsx` - главная страница курса
- `/app/pt/lesson/[day]/[token]/page.tsx` - страница урока
- `/app/pt/lesson/[day]/[token]/overview/page.tsx` - обзор урока

### Компоненты курса
- `/components/subscription/` - все компоненты курса
  - `LessonContent.tsx`
  - `TaskCard.tsx`
  - `tasks/VocabularyTask.tsx`
  - `tasks/RulesTask.tsx`
  - `tasks/ListeningTask.tsx`
  - `tasks/AttentionTask.tsx`
  - `tasks/WritingTask.tsx`
  - `tasks/VocabularyTaskPlayer.tsx`

### API курса
- `/app/api/subscription/` - все API для курса
- `/app/api/admin/lessons/` - API для управления уроками
- `/app/api/admin/levels/` - API для управления уровнями (будет создано)

### Админ-панель курса
- `/app/admin/` - админ-панель (можно расширять для CRM)
- `/components/admin/` - компоненты админ-панели

### База данных
- `/database/subscription-schema.sql` - схема для курса
- Таблицы: `lessons`, `levels`, `audio_files`, `user_progress`, `task_progress`

### Скрипты курса
- `/scripts/import-lessons.js` - импорт уроков (будет обновлен)
- `/scripts/generate-audio-lesson-*.js` - генерация аудио для уроков
- `/scripts/upload-audio-lesson-*.js` - загрузка аудио для уроков

### Файлы уроков
- `/Subsription/` - YAML файлы уроков (будет заменено на JSON)

### Утилиты курса
- `/lib/lesson-translations.ts` - утилиты для переводов уроков
- `/lib/send-lesson-email.ts` - отправка email с уроками

---

## ❌ НЕЛЬЗЯ ТРОГАТЬ (Тренажер /clusters)

### Страницы тренажера
- `/app/clusters/page.tsx` - страница тренажера
- `/app/phrases/page.tsx` - словарь (используется и тренажером, и курсом - осторожно!)
- `/app/subcategories/page.tsx` - подкатегории
- `/app/player/page.tsx` - плеер
- `/app/methodology/page.tsx` - методология

### Компоненты тренажера
- `/components/LanguageSelector.tsx` - используется везде (осторожно!)

### API тренажера
- Нет отдельного API для тренажера (использует общие таблицы)

### База данных тренажера
- Таблицы: `clusters`, `phrases` (используется и курсом!)
- `/database/schema.sql` - основная схема (осторожно!)

### Скрипты тренажера
- `/scripts/import-clusters.js`
- `/scripts/import-*.js` - все скрипты импорта для тренажера
- `/scripts/generate-audio-*.js` - генерация аудио для тренажера (кроме lesson-*)

### Файлы тренажера
- `/Clasters/` - JSON файлы кластеров
- `/Categories/` - категории

### Утилиты тренажера
- `/lib/cluster-config.ts` - конфигурация кластеров
- `/lib/language-context.ts` - контекст языка (используется везде - осторожно!)

---

## ⚠️ ОСТОРОЖНО (Используется и курсом, и тренажером)

### Общие компоненты
- `/components/LanguageSelector.tsx` - селектор языка
- `/lib/language-context.tsx` - контекст языка

### Общие таблицы БД
- `phrases` - слова и фразы (используется и курсом, и тренажером)
  - ✅ Можно добавлять новые записи из курса
  - ❌ Нельзя менять структуру таблицы
  - ❌ Нельзя удалять существующие записи

### Общие утилиты
- `/lib/supabase.ts` - клиент Supabase

---

## 📝 Правила работы

1. **Всегда проверяй**, что файл относится к курсу (`/pt/`, `/subscription/`, `/admin/lessons/`)
2. **Не трогай** файлы, связанные с `/clusters/`
3. **Осторожно** с общими файлами (`phrases`, `LanguageSelector`)
4. **При сомнениях** - спроси или проверь использование файла

---

## 🔍 Как проверить, можно ли трогать файл

```bash
# Проверить, используется ли файл в тренажере
grep -r "clusters" /path/to/file

# Проверить, используется ли файл в курсе
grep -r "pt/lesson" /path/to/file
grep -r "subscription" /path/to/file
```

