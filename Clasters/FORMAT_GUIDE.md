# Формат JSON файлов для кластеров

## ✅ Поддерживаемый формат (как в кластере "Новичок")

Этот формат **уже поддерживается** скриптом импорта и рекомендуется для всех кластеров.

### Структура файла:

```json
{
  "cluster_id": 1,
  "cluster_name": "Реакции и ответы",
  "language": "pt-PT",
  "translation_language": "ru",
  "phrases": [
    {
      "pt": "Acho que isso faz sentido para mim.",
      "ipa": "ˈaʃu ki ˈisu fɐj sɨˈtẽdu pɐɾɐ mĩ",
      "ru": "Думаю, в этом есть смысл для меня.",
      "en": "I think this makes sense to me."
    },
    {
      "pt": "Não tenho a certeza se concordo totalmente.",
      "ipa": "nɐ̃w ˈtɐɲu ɐ sɨɾˈtezɐ sɨ kõˈkoɾdu totɐɫˈmẽtɨ",
      "ru": "Я не уверен(а), что полностью с этим согласен(на).",
      "en": "I'm not sure if I totally agree."
    }
  ]
}
```

### Обязательные поля:

- **`cluster_id`** (number) - ID кластера
- **`cluster_name`** (string) - Название кластера
- **`language`** (string) - Язык фраз (`"pt-PT"`)
- **`translation_language`** (string) - Язык основного перевода (`"ru"`)
- **`phrases`** (array) - Массив фраз

### Поля для каждой фразы:

#### Обязательные:
- **`pt`** (string) - Португальский текст фразы

#### Рекомендуемые (для добавления):
- **`ipa`** (string) - IPA транскрипция (например: `"ˈaʃu ki ˈisu"`)
- **`ru`** (string) - Русский перевод
- **`en`** (string) - Английский перевод

#### Опциональные (можно оставить, но не используются в импорте):
- **`register`** (string) - Регистр речи (`"neutral"`, `"formal"`, `"informal"`)
- **`explicit`** (boolean) - Является ли фраза явной/неприличной

### Пример для кластера "Фразы из фильмов":

```json
{
  "pt": "Que a força esteja contigo.",
  "ipa": "kɨ ɐ ˈfoɾsɐ ɨʃˈtɐjʒɐ kõˈtigu",
  "ru": "Да пребудет с тобой сила.",
  "en": "May the Force be with you.",
  "movie": {
    "title_pt": "Guerra das Estrelas",
    "character": "Obi-Wan Kenobi",
    "year": 1977
  }
}
```

## 📝 Примечания:

1. **IPA транскрипция (`ipa`)** - поле опциональное, но рекомендуется добавлять
2. **Английский перевод (`en`)** - поле опциональное, но рекомендуется добавлять
3. **Русский перевод (`ru`)** - обязательное поле (основной язык перевода)
4. Поля `register` и `explicit` можно оставить в файлах, но они не импортируются в базу данных
5. Для кластера "Фразы из фильмов" можно добавлять объект `movie` с полями `title_pt`, `character`, `year`

## 🔄 План действий:

1. Добавьте поля `ipa` и `en` во все фразы во всех кластерах
2. Сохраните файлы в формате JSON (валидный JSON)
3. Запустите импорт: `node scripts/import-clusters.js`

