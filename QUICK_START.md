# FaloClaro - Быстрый старт после создания Supabase проекта

## ✅ Шаг 1: Применить схему базы данных

1. В Supabase проекте FaloClaro откройте **SQL Editor** (иконка в левом меню)
2. Нажмите **"New Query"**
3. Скопируйте весь код из файла `database/schema.sql`
4. Вставьте в SQL Editor
5. Нажмите **"Run"** (или Cmd/Ctrl + Enter)
6. Должно появиться сообщение об успешном выполнении

**Проверка:** Перейдите в **Table Editor** - должны появиться таблицы:
- `clusters`
- `phrases`
- `translations`

## ✅ Шаг 2: Получить API ключи

1. В Supabase проекте откройте **Settings** (шестеренка внизу слева)
2. Выберите **API**
3. Скопируйте:
   - **Project URL** (под "Project URL")
   - **anon public** ключ (под "Project API keys" → "anon public")

## ✅ Шаг 3: Настроить .env.local

1. В проекте создайте файл `.env.local`:

```bash
cd "/Users/yes/Downloads/YES PROJECTS/FaloClaro/faloclaro-app"
cp env.example .env.local
```

2. Откройте `.env.local` и вставьте ваши ключи:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://youvkbqaruadfpqxxbwi.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_ключ_здесь
```

## ✅ Шаг 4: Создать Storage bucket для аудио

1. В Supabase проекте откройте **Storage** (иконка в левом меню)
2. Нажмите **"Create a new bucket"**
3. Название: `audio`
4. **ВАЖНО:** Включите **"Public bucket"** (переключатель)
5. Нажмите **"Create bucket"**

## ✅ Шаг 5: Добавить тестовые данные (опционально)

### Добавить кластер:

В **SQL Editor** выполните:

```sql
INSERT INTO clusters (name, description, order_index)
VALUES ('Приветствия', 'Основные приветствия и знакомство', 1);
```

### Добавить фразу (после получения cluster_id):

```sql
-- Сначала получите ID кластера из Table Editor, затем:
INSERT INTO phrases (cluster_id, portuguese_text, ipa_transcription, audio_url, order_index)
VALUES (
  'ваш-cluster-id-здесь',
  'Olá, como está?',
  'ˈɔlɐ ˈkomu ɨʃˈta',
  'https://youvkbqaruadfpqxxbwi.supabase.co/storage/v1/object/public/audio/test.mp3',
  1
);
```

### Добавить перевод:

```sql
-- Используйте phrase_id из предыдущего шага:
INSERT INTO translations (phrase_id, language_code, translation_text)
VALUES (
  'ваш-phrase-id-здесь',
  'en',
  'Hello, how are you?'
);
```

## ✅ Шаг 6: Запустить локально

```bash
cd "/Users/yes/Downloads/YES PROJECTS/FaloClaro/faloclaro-app"
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## ⚠️ Важно про "EXCEEDING USAGE LIMITS"

Если видите предупреждение о превышении лимитов:
- Это может быть связано с другими проектами в аккаунте
- Для тестирования FaloClaro это не критично
- При необходимости можно перейти на платный план или оптимизировать использование

## 🎯 Что дальше?

1. Загрузите аудио файлы в Storage bucket `audio`
2. Добавьте больше кластеров и фраз
3. Настройте GitHub репозиторий
4. Задеплойте на Vercel

См. `SETUP.md` для подробных инструкций.


