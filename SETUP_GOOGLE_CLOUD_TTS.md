# Пошаговая настройка Google Cloud TTS

## ✅ Шаг 1: API уже включен (готово!)

Вы уже включили Cloud Text-to-Speech API. Отлично!

---

## 📝 Шаг 2: Создание Service Account

1. **В левом меню Google Cloud Console** найдите и нажмите на:
   - **"IAM & Admin"** (или "IAM и администрирование")
   - Затем **"Service Accounts"** (или "Учетные записи служб")

2. **Нажмите кнопку "CREATE SERVICE ACCOUNT"** (или "СОЗДАТЬ УЧЕТНУЮ ЗАПИСЬ СЛУЖБЫ") вверху страницы

3. **Заполните форму:**
   - **Service account name:** `tts-service` (или любое другое имя)
   - **Service account ID:** заполнится автоматически
   - **Description (опционально):** `Service account for FaloClaro TTS`
   - Нажмите **"CREATE AND CONTINUE"** (или "СОЗДАТЬ И ПРОДОЛЖИТЬ")

4. **Назначьте роль:**
   - В поле **"Grant this service account access to project"** начните вводить: `Text-to-Speech`
   - Выберите роль: **"Cloud Text-to-Speech API User"**
   - Нажмите **"CONTINUE"** (или "ПРОДОЛЖИТЬ")

5. **Пропустите следующий шаг:**
   - Нажмите **"DONE"** (или "ГОТОВО")
   - Можно пропустить шаг с "Grant users access to this service account"

---

## 🔑 Шаг 3: Создание и скачивание JSON ключа

1. **На странице Service Accounts** вы увидите список учетных записей
   - Найдите созданную учетную запись `tts-service`
   - **Нажмите на неё** (кликните на email или имя)

2. **Перейдите на вкладку "KEYS"** (или "КЛЮЧИ"):
   - Вверху страницы есть вкладки: "PERMISSIONS", "KEYS", "DETAILS"
   - Нажмите на **"KEYS"**

3. **Создайте новый ключ:**
   - Нажмите кнопку **"ADD KEY"** (или "ДОБАВИТЬ КЛЮЧ")
   - Выберите **"Create new key"** (или "Создать новый ключ")

4. **Выберите формат:**
   - В появившемся окне выберите **"JSON"**
   - Нажмите **"CREATE"** (или "СОЗДАТЬ")

5. **Файл автоматически скачается:**
   - JSON файл скачается в папку "Загрузки" (Downloads)
   - Имя файла будет что-то вроде: `surprize-470418-xxxxx.json`

---

## 📁 Шаг 4: Сохранение ключа в проект

1. **Переименуйте файл:**
   - Найдите скачанный JSON файл в папке "Загрузки"
   - Переименуйте его в: `google-credentials.json`

2. **Переместите файл в проект:**
   - Скопируйте `google-credentials.json` в папку проекта:
   ```
   /Users/yes/Downloads/YES PROJECTS/FaloClaro/faloclaro-app/
   ```

3. **Проверьте расположение:**
   - Файл должен быть здесь:
   ```
   /Users/yes/Downloads/YES PROJECTS/FaloClaro/faloclaro-app/google-credentials.json
   ```

---

## ✅ Шаг 5: Проверка

Убедитесь, что файл на месте:

```bash
cd "/Users/yes/Downloads/YES PROJECTS/FaloClaro/faloclaro-app"
ls -la google-credentials.json
```

Если файл есть, вы увидите его в списке.

---

## 🚀 Шаг 6: Генерация аудио

После выполнения всех шагов, запустите:

```bash
cd "/Users/yes/Downloads/YES PROJECTS/FaloClaro/faloclaro-app"
export GOOGLE_APPLICATION_CREDENTIALS="./google-credentials.json"
node scripts/generate-audio-simple.js
```

---

## ⚠️ Важно

- **НЕ коммитьте** `google-credentials.json` в Git (он уже в .gitignore)
- **НЕ делитесь** этим файлом публично
- Файл содержит секретные ключи доступа к вашему Google Cloud проекту

---

## 🆘 Если что-то не работает

1. **Проверьте, что API включен:**
   - APIs & Services → Enabled APIs
   - Должен быть "Cloud Text-to-Speech API" со статусом "Enabled"

2. **Проверьте права Service Account:**
   - Service Account → Permissions
   - Должна быть роль "Cloud Text-to-Speech API User"

3. **Проверьте путь к файлу:**
   - Убедитесь, что `google-credentials.json` в корне проекта
   - Проверьте, что переменная окружения установлена правильно



