# Быстрая настройка Stripe для FaloClaro

## 📋 Пошаговая инструкция

### Шаг 1: Получение API ключа из Stripe Dashboard

1. **Откройте Stripe Dashboard**: https://dashboard.stripe.com
2. **Перейдите в раздел API keys**:
   - В левом меню найдите **"Developers"** (иконка с символом `</>`)
   - Нажмите на **"API keys"**
3. **Найдите секретный ключ**:
   - В разделе **"Secret key"** найдите ключ для **Live mode** (боевой режим)
   - Ключ начинается с `sk_live_`
   - Нажмите **"Reveal live key"** чтобы увидеть ключ
   - **Скопируйте ключ** (он будет показан только один раз!)

### Шаг 2: Добавление ключа в проект

#### Для локальной разработки:

1. Создайте файл `.env.local` в корне проекта (если его нет)
2. Добавьте следующие строки:

```env
STRIPE_SECRET_KEY=sk_live_ваш_ключ_здесь
NEXT_PUBLIC_BASE_URL=https://www.faloclaro.com
```

**Важно:** Замените `sk_live_ваш_ключ_здесь` на реальный ключ, который вы скопировали из Stripe Dashboard.

#### Для продакшена (Vercel):

1. Откройте ваш проект на Vercel: https://vercel.com
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте две переменные:
   - **Name:** `STRIPE_SECRET_KEY` | **Value:** `sk_live_ваш_ключ`
   - **Name:** `NEXT_PUBLIC_BASE_URL` | **Value:** `https://www.faloclaro.com`
4. Нажмите **Save**
5. **Перезапустите деплой** (Redeploy)

### Шаг 3: Проверка работы

1. Запустите проект: `npm run dev`
2. Откройте страницу: http://localhost:3000/methodology
3. Прокрутите вниз - должен появиться блок **"Оставьте чаевые"**
4. Нажмите на блок, чтобы развернуть форму
5. Выберите сумму (€1, €10, €100 или введите свою)
6. Нажмите **"Оплатить"**
7. Должно произойти перенаправление на страницу оплаты Stripe

## ✅ Готово!

Теперь донаты работают. Пользователи могут оставлять чаевые на странице "О методе".

## 🔒 Безопасность

- **Никогда не коммитьте** `.env.local` в Git
- Файл `.env.local` уже добавлен в `.gitignore`
- Используйте боевые ключи (`sk_live_`) только в продакшене
- Для тестирования можно использовать тестовые ключи (`sk_test_`)

## 📝 Где найти ключи в Stripe Dashboard

```
Stripe Dashboard
  └── Developers (в левом меню)
      └── API keys
          └── Secret key (Live mode)
              └── Reveal live key → скопировать ключ
```

## ❓ Проблемы?

Если что-то не работает:
1. Проверьте, что ключ правильно скопирован (без пробелов)
2. Убедитесь, что используется ключ для Live mode (не Test mode)
3. Проверьте консоль браузера на наличие ошибок
4. Убедитесь, что переменные окружения установлены в Vercel

