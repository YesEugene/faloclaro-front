# 🚀 Деплой FaloClaro в Production

Пошаговая инструкция для размещения приложения в интернете.

## 📋 Предварительные требования

- ✅ Аккаунт на [Supabase](https://app.supabase.com)
- ✅ Аккаунт на [GitHub](https://github.com)
- ✅ Аккаунт на [Vercel](https://vercel.com)
- ✅ Supabase проект создан и настроен
- ✅ База данных заполнена (кластеры, фразы, переводы, аудио)

## 🔧 Шаг 1: Подготовка Git репозитория

### 1.1. Проверьте текущий статус

```bash
cd "/Users/yes/Downloads/YES PROJECTS/FaloClaro/faloclaro-app"
git status
```

### 1.2. Добавьте все файлы (кроме .env.local и google-credentials.json)

```bash
# Добавьте все файлы
git add .

# Исключите чувствительные файлы (они уже в .gitignore)
git reset HEAD .env.local google-credentials.json 2>/dev/null || true
```

### 1.3. Создайте коммит

```bash
git commit -m "🚀 Production ready: FaloClaro Portuguese learning app"
```

### 1.4. Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Название: `faloclaro-app` (или другое уникальное имя)
4. **НЕ** инициализируйте с README (у нас уже есть)
5. Нажмите "Create repository"

### 1.5. Подключите удаленный репозиторий

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/faloclaro-app.git
git branch -M main
git push -u origin main
```

## 🌐 Шаг 2: Настройка Vercel

### 2.1. Войдите в Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub

### 2.2. Создайте новый проект

1. Нажмите "Add New..." → "Project"
2. Выберите репозиторий `faloclaro-app`
3. Нажмите "Import"

### 2.3. Настройте проект

**Framework Preset:** Next.js (должен определиться автоматически)

**Root Directory:** `./` (оставьте по умолчанию)

**Build Command:** `npm run build` (по умолчанию)

**Output Directory:** `.next` (по умолчанию)

**Install Command:** `npm install` (по умолчанию)

### 2.4. Добавьте Environment Variables

**ВАЖНО:** Используйте переменные из вашего Supabase проекта (НЕ из FlipTrip!)

1. В разделе "Environment Variables" добавьте:

```
NEXT_PUBLIC_SUPABASE_URL = https://your-faloclaro-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY = your-faloclaro-anon-key
```

2. Выберите все окружения:
   - ✅ Production
   - ✅ Preview
   - ✅ Development

3. Нажмите "Save"

### 2.5. Деплой

1. Нажмите "Deploy"
2. Дождитесь завершения сборки (обычно 1-2 минуты)
3. После успешного деплоя вы получите URL вида: `https://faloclaro-app.vercel.app`

## ✅ Шаг 3: Проверка после деплоя

### 3.1. Проверьте основные страницы

- ✅ Главная страница: `/clusters`
- ✅ Страница фраз: `/phrases?clusters=...`
- ✅ Плеер: `/player?phraseId=...`
- ✅ Тест: `/test`
- ✅ О методе: `/methodology`

### 3.2. Проверьте функциональность

- ✅ Выбор кластеров
- ✅ Загрузка фраз
- ✅ Воспроизведение аудио
- ✅ Переключение языков
- ✅ Свайп на странице плеера
- ✅ Настройки воспроизведения

### 3.3. Проверьте на мобильных устройствах

Откройте приложение на телефоне и проверьте:
- ✅ Адаптивность
- ✅ Свайп-жесты
- ✅ Качество аудио

## 🔒 Шаг 4: Настройка безопасности

### 4.1. Проверьте Supabase RLS политики

Убедитесь, что:
- ✅ Таблицы `clusters`, `phrases`, `translations` доступны для чтения всем
- ✅ Storage bucket `audio` публичный
- ✅ Нет лишних прав на запись

### 4.2. Проверьте CORS настройки Supabase

В Supabase Dashboard → Settings → API:
- ✅ Разрешите домен Vercel в CORS settings (если требуется)

## 📱 Шаг 5: Опционально - Настройка кастомного домена

### 5.1. Добавьте домен в Vercel

1. В настройках проекта Vercel → Domains
2. Добавьте ваш домен (например, `faloclaro.com`)
3. Следуйте инструкциям для настройки DNS

## 🐛 Troubleshooting

### Ошибка: "Missing Supabase environment variables"

**Решение:**
- Проверьте, что переменные добавлены в Vercel
- Убедитесь, что выбраны все окружения (Production, Preview, Development)
- Пересоберите проект после добавления переменных

### Ошибка: "Failed to fetch phrases"

**Решение:**
- Проверьте, что Supabase URL и ключ правильные
- Проверьте RLS политики в Supabase
- Проверьте, что данные загружены в базу

### Аудио не воспроизводится

**Решение:**
- Проверьте, что Storage bucket `audio` публичный
- Проверьте, что `audio_url` в базе данных правильные
- Проверьте CORS настройки Supabase Storage

### Сборка падает с ошибкой TypeScript

**Решение:**
```bash
# Локально проверьте сборку
npm run build

# Исправьте все ошибки перед деплоем
```

## 📊 Мониторинг

После деплоя следите за:
- Логами в Vercel Dashboard → Deployments
- Метриками производительности
- Ошибками в браузере (Console)

## 🔄 Обновление приложения

После изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

Vercel автоматически задеплоит новую версию!

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Vercel Dashboard
2. Проверьте консоль браузера
3. Убедитесь, что все переменные окружения настроены правильно

---

**Готово!** 🎉 Ваше приложение теперь доступно в интернете!



